# Project preferences

After app changes, always build locally with `./build.sh` so the user can try the app without waiting for GitHub Actions. Continue publishing releases through the existing workflow.
